# چک‌لیست آپلود بعد از بیلد (هاست اشتراکی)

## فلوی کاری
1. **اینجا (لوکال):** تغییرات را بزن، بیلد بگیر، فایل‌های زیر را به هاست آپلود کن.
2. **هاست:** بیلد اجرا **نمی‌شود**؛ فقط اجرا با `npm start` (همان `node server.js`).

---

## بعد از هر بیلد موفق — این‌ها را آپلود کن

| مورد | مسیر | توضیح |
|------|------|--------|
| خروجی بیلد | **`.next`** (کل پوشه) | حتماً — وگرنه سایت قدیمی می‌ماند. |
| سرور نود | **`server.js`** | حتماً. |
| پکیج‌ها | **`package.json`** و **`package-lock.json`** | حتماً (برای `npm install` در صورت نیاز). |
| فایل env | **`.env`** یا **`.env.production`** | در صورت تغییر تنظیمات؛ معمولاً فقط روی هاست ویرایش می‌کنی. |
| فایل‌های استاتیک | **`public`** (در صورت تغییر) | تصاویر، ویدیو، آپلودهای عمومی. |

اگر کد سمت سرور (مثلاً API یا middleware) عوض کرده‌ای، علاوه بر `.next` این‌ها را هم بفرست:
- **`app`** (کل پوشه)
- **`components`**
- **`store`**
- **`lib`**
- **`middleware.ts`**
- **`next.config.js`**
- **`hooks`** (در صورت وجود)

هاست برای اجرا به **`node_modules`** هم نیاز دارد. دو حالت داریم:
- **بار اول روی هاست:** یک بار `npm install --production` یا `npm install --legacy-peer-deps` بزن (یا از cPanel Node App اجرا کن).
- **آپدیت‌های بعدی:** فقط همین فایل‌های بالا را جایگزین کن؛ معمولاً نیازی به آپلود دوبارهٔ `node_modules` نیست.

---

## بیلد لوکال (ویندوز)

در پوشهٔ پروژه:

```bat
npm run build
```

اگر `cross-env` خطا داد، از اسکریپت **`build-then-upload.bat`** استفاده کن؛ یا دستی:

```powershell
$env:NEXT_PRIVATE_SKIP_TURBO="1"; npx next build --webpack
```

---

## روی هاست بعد از آپلود

1. اگر `node_modules` نداری: یک بار `npm install --production` یا طبق دستور cPanel.
2. ریستارت اپ: از cPanel Node.js App → Restart، یا اگر دستی اجرا می‌کنی دوباره `npm start` بزن.

---

## نکات

- **همیشه قبل از آپلود بیلد بگیر** تا `.next` به‌روز باشد.
- **`.env`** را با اطلاعات حساس لوکال روی هاست بازنویسی کن؛ از لوکال آپلود نکن.
- اگر خطای ۵۰۳ یا ماژول پیدا نشد: مطمئن شو کل `.next` و در صورت نیاز `app` / `components` / `server.js` / `next.config.js` را فرستاده‌ای و روی هاست ریستارت کرده‌ای.
