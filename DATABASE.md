# ذخیره‌سازی داده‌ها در دیتابیس

تمام داده‌های مهم این پروژه در **MySQL** ذخیره می‌شوند. هیچ داده‌ای فقط در مرورگر (localStorage) به‌صورت دائمی نگه داشته نمی‌شود.

## جداول اصلی

| جدول | کاربرد |
|------|--------|
| `products` | محصولات |
| `vehicles` | خودروها و مدل‌ها |
| `categories` | دسته‌بندی‌ها |
| `orders` | سفارش‌ها |
| `users` | کاربران |
| `carts` | سبد خرید (هم مهمان هم کاربر لاگین‌شده) |
| `quick_buy_chats` | چت خرید سریع |
| `chat_messages` | پیام‌های چت |
| `settings` / `site_settings` | تنظیمات سایت |
| و سایر جداول در اسکریپت setup | ... |

## راه‌اندازی (یک بار)

1. **MySQL** را نصب و اجرا کنید (یا XAMPP).
2. دیتابیس بسازید:
   ```sql
   CREATE DATABASE saded CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
3. در ریشه پروژه فایل **`.env.local`** بسازید و متغیرها را تنظیم کنید:
   ```env
   DB_HOST=localhost
   DB_PORT=3306
   DB_NAME=saded
   DB_USER=root
   DB_PASSWORD=رمز_خودتان
   ```
4. اسکریپت ایجاد جداول را اجرا کنید:
   ```bash
   npm run setup-mysql
   ```

بعد از این مراحل، همهٔ داده‌ها (خودروها، سبد خرید، سفارش‌ها، محصولات، تنظیمات و ...) در دیتابیس ذخیره و بعد از رفرش یا بستن مرورگر از بین نمی‌روند.

## نکته

اگر دیتابیس در دسترس نباشد، APIها خطای **۵۰۳** برمی‌گردانند و داده‌ای ذخیره یا بارگذاری نمی‌شود تا از از دست رفتن یا ذخیرهٔ موقت در مرورگر جلوگیری شود.
